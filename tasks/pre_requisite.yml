---
# /etc not exists, something wrong with this system
- name: nsswitch | Check /etc exists
  ansible.builtin.stat:
    path: "/etc"
  register: "etc_stat"
  failed_when: "etc_stat.stat.exists == False"
